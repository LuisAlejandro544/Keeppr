feat/refactor: personalización de tipografía por nota y por fragmento de texto (inline) en lugar de ajuste global

- Migrado el selector de tipografías desde un estado global en SharedPreferences hacia un modelo granular por nota y por fragmentos de texto seleccionados:
  * Entidad `Note` actualizada con el campo `fontTheme` y migración de base de datos Room (`MIGRATION_1_2` en `AppDatabase.kt` a versión 2).
  * Diálogo `FontSelectionDialog` adaptado con selector de alcance dual: "Toda la nota" (actualiza la tipografía base de la nota activa) y "Texto seleccionado" (envuelve o inserta la etiqueta `[font:id]...[/font]` en la posición del cursor).
  * Renderizador `MarkdownRenderer.kt` enriquecido con soporte para etiquetas `[font:id]...[/font]`, permitiendo combinar múltiples estilos tipográficos (Serif, Sans, Mono, Cursiva, Predeterminada) en una misma nota en vista previa interactiva.
  * Agregado botón "Aa Fuente" en `MarkdownToolbar.kt` para acceso directo y ergonómico desde pantallas móviles.
  * Tarjetas de notas en `NotesListScreen.kt` renderizan su título y extracto con la tipografía asignada individualmente a cada nota.
- Mantenidas las correcciones previas de rendimiento y estabilidad:
  * Corregido el targetState en `AnimatedContent` de `MainActivity.kt`: conmuta según `(activeNote != null)` para prevenir el desmontaje de la pantalla, transiciones innecesarias y el cierre forzado del teclado virtual (IME).
  * Desacoplado el scroll forzado en `onValueChange` en `NoteEditorScreen.kt` y sincronizado al ViewModel únicamente cuando el texto cambia de manera efectiva.
  * Optimizada la lista de notas (`NotesListScreen.kt`) memorizando la partición de notas fijadas y métricas por tarjeta.
  * Integración de Rust nativo (`lib.rs`, `native-bridge.cpp`, `NativeEngine.kt`) para cálculo en una sola pasada de resúmenes de Markdown (`rust_process_note_summary`) y búsqueda insensible a mayúsculas (`rust_match_note`).


