fix(proguard): corregir sintaxis de assumenosideeffects para java.lang.Throwable en r8

- Corrección de Parseo en R8 / ProGuard (`app/proguard-rules.pro`):
  * Añadida la palabra clave `class` faltante en la regla de poda de trazas de pila: `-assumenosideeffects class java.lang.Throwable { public void printStackTrace(); }`.
  * Resuelve el fallo de compilación en GitHub Actions (`minifyReleaseWithR8 FAILED: Expected [!]interface|@interface|class|enum`).
  * Permite a R8 minificar y compilar exitosamente la variante de producción Release sin errores de sintaxis.
